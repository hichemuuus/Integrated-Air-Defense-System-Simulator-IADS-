{
  "identifier": "default",
  "description": "Default permissions for Syntra Command",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "shell:allow-open",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
